"use client";
import {CalendarDays,ChevronLeft,ChevronRight,RotateCcw} from "lucide-react";
import styles from "./CentralDateSelector.module.css";
const label=(value:string)=>new Intl.DateTimeFormat("es-ES",{timeZone:"UTC",day:"numeric",month:"long",year:"numeric"}).format(new Date(`${value}T12:00:00Z`));
const move=(value:string,days:number)=>{const date=new Date(`${value}T12:00:00Z`);date.setUTCDate(date.getUTCDate()+days);return date.toISOString().slice(0,10)};
export default function CentralDateSelector({value,today,loading,onChange}:{value:string;today:string;loading:boolean;onChange:(date:string)=>void}){const isToday=value===today;return <section className={styles.bar} aria-label="Seleccionar fecha del resumen"><div><small>RESUMEN DEL DÍA</small><strong>{isToday?"Hoy":label(value)}</strong><span>Europe/Madrid</span></div><div className={styles.controls}><button aria-label="Día anterior" onClick={()=>onChange(move(value,-1))}><ChevronLeft/></button><label><CalendarDays/><input type="date" value={value} max={today} onChange={event=>event.target.value&&onChange(event.target.value)}/></label><button aria-label="Día siguiente" disabled={isToday} onClick={()=>onChange(move(value,1))}><ChevronRight/></button><button className={styles.today} disabled={isToday} onClick={()=>onChange(today)}><RotateCcw/> HOY</button></div>{loading?<span className={styles.loading}>Cargando resumen…</span>:null}</section>}
